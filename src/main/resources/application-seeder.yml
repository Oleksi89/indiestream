spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    org.hibernate.SQL: warn
    org.hibernate.orm.results.mapping: warn
    org.hibernate.type.descriptor.sql.BasicBinder: warn